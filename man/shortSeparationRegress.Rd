% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/short-separation.R
\name{shortSeparationRegress}
\alias{shortSeparationRegress}
\title{Regress governed short-separation nuisance signals}
\usage{
shortSeparationRegress(
  x,
  assay_name = "OD",
  short = NULL,
  method = c("nearest", "all"),
  lag_seconds = 0,
  output_assay = paste0(assay_name, "_ssr"),
  condition_limit = 1e+10
)
}
\arguments{
\item{x}{A governed OD or pair-collapsed haemoglobin
\code{PhysioExperiment}.}

\item{assay_name}{Exact source assay name.}

\item{short}{A compatible \code{nirs_short_channels} map returned by
\code{identifyShortChannels()}, or \code{NULL} to use the strict 10-mm default.}

\item{method}{Exactly \code{"nearest"} or \code{"all"}.}

\item{lag_seconds}{A finite lag that corresponds to an exact sample count.}

\item{output_assay}{Exact name for the added corrected assay.}

\item{condition_limit}{Finite regression condition-number limit greater
than one.}
}
\value{
A clone of \code{x} with one short-separation-corrected assay.
}
\description{
Fits a centred short-channel regression separately for each long channel.
Short channels are copied unchanged. Positive lag means that the short
signal at \code{t - lag_seconds} predicts the long signal at \code{t}.
}
\details{
For a target \code{y} and selected short signals \code{X}, the fitted
intercept is retained and only \verb{(X - colMeans(X)) beta} is subtracted.
Regression uses a deterministic SVD solve and rejects rank-deficient or
over-limit designs. Short columns and lag-edge rows remain unchanged.
}
\references{
Saager and Berger (2005), DOI: 10.1364/JOSAA.22.001874.
}
