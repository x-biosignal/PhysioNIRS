% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motion-spline.R
\name{splineMotionCorrect}
\alias{splineMotionCorrect}
\title{Correct detected fNIRS motion intervals with cubic smoothing splines}
\usage{
splineMotionCorrect(
  x,
  mask = NULL,
  assay_name = "OD",
  output_assay = paste0(assay_name, "_spline"),
  p = 0.99,
  t_motion = 0.5,
  t_mask = 1,
  sd_threshold = 20,
  amplitude_threshold = 0.5,
  group_wavelengths = TRUE
)
}
\arguments{
\item{x}{A governed \code{PhysioExperiment}.}

\item{mask}{A compatible \code{nirs_motion_mask}, or \code{NULL} to detect once.}

\item{assay_name}{Exact source assay name.}

\item{output_assay}{Exact new assay name.}

\item{p}{Cubic smoothing parameter in \verb{[0, 1]}.}

\item{t_motion}{Positive detection lag window in seconds, used only when
\code{mask = NULL}.}

\item{t_mask}{Non-negative detection-mask expansion in seconds, used only
when \code{mask = NULL}.}

\item{sd_threshold}{Positive derivative-SD detection multiplier, used only
when \code{mask = NULL}.}

\item{amplitude_threshold}{Positive absolute-change detection threshold,
used only when \code{mask = NULL}.}

\item{group_wavelengths}{Whether detection unions wavelengths from each
source-detector pair, used only when \code{mask = NULL}.}
}
\value{
A clone of \code{x} with one motion-corrected assay.
}
\description{
Uses the MATLAB \code{csaps} smoothing-parameter definition and stitches
corrected artifact and clean segments with short local means.
}
